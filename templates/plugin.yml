# Generated $date
name: $name
main: org.clapper.bukkit.randomteleport.RandomTeleportPlugin
author: Brian M. Clapper
version: $version
depend: ["mc-scala-plugin"]
load: "POSTWORLD"
commands:
  rp:
    description: "Randomly teleport somewhere."
    usage: "/rp"
    permission: org.clapper.mcRandomTeleport.enabled
permissions:
  org.clapper.mcRandomTeleport.enabled:
    description: Whether or not you can use /rp
    default: false
  org.clapper.mcRandomTeleport.immediate:
    description: "Whether or not you can teleport immediately, always."
    default: false
